{
  "name": "setup-cdi-tools",
  "version": "3.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "bun build --outdir ./dist --target node ./src/index.ts",
    "test": "IS_TEST=true bun test",
    "format": "prettier --write src/**/*.ts",
    "format-check": "prettier --check src/**/*.ts"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/tool-cache": "^2.0.1",
    "js-yaml": "^4.1.0",
    "semver": "^7.3.8"
  },
  "devDependencies": {
    "@actions/io": "^1.1.2",
    "@types/node": "^25.0.7",
    "@types/semver": "^7.3.12",
    "prettier": "^2.7.1"
  }
}
